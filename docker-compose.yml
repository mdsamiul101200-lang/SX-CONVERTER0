services:
  sx-converter:
    build: .
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      MAX_FILE_SIZE_MB: 100
      JOB_TIMEOUT_MS: 300000
      DATA_DIR: /app/data
      CORS_ORIGIN: "*"
    volumes:
      - sx-data:/app/data
    restart: unless-stopped

volumes:
  sx-data:
